# 💠 SPHY Tri-Cluster Network: Non-Local Entanglement Visualizer

## 📖 Overview

The **SPHY Tri-Cluster Network Visualizer** is an advanced 3D engine designed to emulate the geometric and quantum entanglement links between three distinct particle clusters (Tri-Body Net). Processing massive coordinate spaces stored in Parquet format, this tool renders subatomic particle behavior (Fermions, Bosons, and Anyons) while visualizing the hyper-dense, non-local connection beams that tie the clusters together.

This module acts as a core topological interface for **Harpia Quantum**, simulating infrastructural synchronization at a Kardashev Type II energy scale.

## 🛠️ Architectural Features

* **Quantum Particle Classification**: Maps particles based on their fundamental structural roles:
* 🟡 **Fermions (`FERMION_UID`)**: Rendered as dense, heavy gold nodes forming the underlying immutable matter.
* 🔵 **Bosons (`BOSON_FORCE`)**: Rendered as cyan filaments representing gauge fields and force carriers.
* 🟣 **Anyons (`ANYON_BRAID`)**: Rendered as magenta traces tracking quasi-particle localized braiding states.


* **Deterministic Sub-Sampling**: Dynamically processes up to 2,000 points per cluster on the fly (`df.sample(2000)`) to maintain rendering stability and real-time fluidity on Unix/Pop!_OS configurations.
* **Entanglement Beams (Tri-Cluster Synchronization)**: Automatically tracks barycentric geometric averages to draw high-intensity pulsating connection lasers between clusters A, B, and C.
* **Automated Mesh Dance**: Slow-drift rotational mechanics (`rotate_x` / `rotate_z`) emulating cosmic thermal equilibrium and quantum state fluctuation.

## 🚀 Getting Started

### Prerequisites

Ensure you have your environment fully initialized with the required execution frameworks:

```bash
pip install py5 pandas pyarrow fastparquet numpy

```

### Execution

1. **Verify State**: Confirm that your tri-cluster data core (`sphy_tri_body_net.parquet`) is present within the local execution path.
2. **Launch Visualizer**:
```bash
python sphy_tri_cluster_viewer.py

```



## 🎮 Interface Controls

| Mechanic | Input Action | Description |
| --- | --- | --- |
| **Orbital Rotate** | Left Click + Drag | Changes the observer point across the 3D grid matrix. |
| **Planar Pan** | Right Click + Drag | Displaces the X and Y mesh viewport offsets. |
| **Depth Scale** | Mouse Wheel (Scroll) | Zooms inside or out of the central entanglement hub. |

## 🧪 Theoretical Foundation

The Tri-Cluster configuration serves to visually map how macroscopic data structures remain correlated over astronomical distances. By tracking the centroids of distinct point-cloud arrays and firing harmonic phase pulses (`py5.remap(np.sin(...))`), the simulation visually proves that regardless of subatomic turbulence, the macro-state entanglement profile remains perfectly stable, symmetric, and non-local.

---

**Developed by:** Deywe Okabe

**Department:** Harpia Quantum Deeptech – Black Swan Research

**Protocol:** SPHY-TriBody Interconnect Framework
