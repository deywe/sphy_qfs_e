# 🌀 SPHY: Einstein-Rosen Centralized Bridge

## 📖 Overview

The **SPHY Einstein-Rosen Visualizer** is a high-fidelity simulation designed to emulated information tunneling through an Einstein-Rosen Bridge (Wormhole). The project integrates General Relativity, Fractal Geometry based on the Golden Ratio ($\phi$), and deterministic data auditing via SHA-256.

This visualizer processes **10 Sovereign Nodes** to demonstrate how information can be transferred non-locally between two distinct points in spacetime without losing integrity.

## 🛠️ SPHY Features

* **Dynamic Relativistic Mesh**: The bridge manifold is calculated in real-time using an `arcsinh` function modulated by the **Breathe Protocol** ($\phi$).
* **Sovereign Node Tunneling**: 10 3D plasma spheres traverse the gravitational throat, shifting phases (Cyan to Magenta) as they cross the singularity.
* **Deterministic Auditing**: Real-time SHA-256 hash display on the HUD, ensuring the visualized data matches the validated kernel.
* **Total Interactivity**: Advanced camera controls (orbital rotation and pan) and dynamic zoom for detailed inspection of spatial curvature.

## 🚀 Getting Started

### Prerequisites

Ensure your Python environment is set up (Optimized for Pop!_OS or Ubuntu):

```bash
pip install py5 pandas pyarrow fastparquet numpy

```

### Execution

Run the main simulation:

```bash
python sphy_einstein_rosen_bridge.py

```

## 🎮 Controls

| Action | Command |
| --- | --- |
| **Rotate** | Left Click + Drag |
| **Pan (Move)** | Right Click + Drag |
| **Zoom** | Mouse Wheel (Scroll) |

## 🧪 Scientific Foundations

The visualizer applies the **Schwarzschild Metric** adapted for the Harpia Network. The bridge "breathing" ($1/\phi$) simulates the harmonic resonance required to maintain the stability of the gravitational throat, preventing the spaghettification of data nodes during transit.

> "In the Harpia network, distance is not a physical obstacle, but a software variable of reality."

---

**Developed by:** Deywe Okabe

**Laboratory:** Harpia Quantum Deeptech – Black Swan Research

**License:** MIT / SPHY Open Protocol
